# Q13. 대여 기록이 한 번도 없는 회원의 이름 조회 (NOT IN 서브쿼리)
# Query:
SELECT name 
FROM member 
WHERE id NOT IN (SELECT DISTINCT member_id FROM rental);

# Result:
+--------+
| name   |
+--------+
| 송태섭 |
| 신준섭 |
| 홍길동 |
+--------+
