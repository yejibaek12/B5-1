# 파일별 작성 체크리스트

---

## 1. `schema.sql` 체크리스트
- [x] 직접 정한 자유 주제로 최소 4개 테이블 생성
- [x] 모든 테이블에 기본키(PK) 지정   
- [x] 테이블 간 1:N 관계 최소 2개 이상 연결 (외래키/FK 설정)   
- [x] NOT NULL 제약조건 1개 이상 적용   
- [x] UNIQUE 제약조건 1개 이상 적용   
- [x] 컬럼 타입을 의미에 맞게 선택 (VARCHAR, INTEGER, DATETIME 등)   
- [x] 테이블 및 컬럼 이름은 역할이 직관적으로 드러나도록 명명   
- [x] 인덱스(`CREATE INDEX`) 1개 이상 생성 및 적용 이유 주석 작성   
- [x] 실행 순서대로 정렬하여 파일 1개로 실행 가능하도록 구성

---

## 2. `insert.sql` 체크리스트
- [x] 각 테이블당 최소 10행(Row) 이상 데이터 입력   
- [x] 의미 있는 테스트용 샘플 데이터로 구성   
- [x] 외래키(FK) 제약조건 오류 방지를 위해 부모 테이블 데이터부터 순서대로 입력

---

## 3. `queries.sql` 체크리스트
- [x] 각 쿼리 상단에 "무엇을 확인하는 쿼리인지" 한 줄 설명 주석 작성   
- [x] 총 15개 이상의 핵심 쿼리 작성   
- [x] 기본 조회 4개 이상 (WHERE, ORDER BY, LIMIT 포함)   
- [x] 조인 4개 이상 (INNER JOIN 2개 이상, LEFT JOIN 1개 이상 포함)   
- [x] 집계 3개 이상 (COUNT, SUM, AVG 중 2개 이상 + GROUP BY 포함)   
- [x] 서브쿼리 1개 이상 포함   
- [x] 데이터 수정 및 삭제 2개 이상 (UPDATE, DELETE 포함)   
- [x] 특정 DB 고유 문법 사용 시 해당 쿼리에 주석으로 명시 

---

## 4. `screenshots/` (및 결과 텍스트) 체크리스트
- [x] 17개 쿼리의 실행 결과를 확인할 수 있는 자료 준비   
- [x] 각 실행 결과를 스크린샷 이미지 또는 결과 텍스트 파일로 저장   
- [x] 파일명을 `query_01_result` 등과 같이 매칭하기 쉽게 명명하여 폴더에 저장